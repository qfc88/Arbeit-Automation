#
# Production overlay for home-server deployment (Ryzen 5700X + 16GB + RTX 2060).
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# What this overlay does vs. the base compose:
#   - Every service port is rebound to 127.0.0.1 so nothing is reachable from
#     the LAN unless it goes through nginx (which is the single public ingress).
#   - nginx listens on 0.0.0.0 and fronts scraper/admin/monitor; public traffic
#     reaches nginx via the WireGuard tunnel from the VPS; admin traffic
#     reaches nginx via the ZeroTier interface.
#   - ml-inference uses the NVIDIA runtime (requires nvidia-container-toolkit).
#   - All services get restart: unless-stopped.
#
# Services api / frontend / ml-inference are built in Pha 3; their stanzas are
# placeholders commented out until their Dockerfiles exist.
#

services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
    restart: unless-stopped

  job-scraper:
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
    environment:
      SCRAPER_USE_PARALLEL: "true"
      SCRAPER_CONCURRENCY: "6"

  adminer:
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped

  monitor:
    ports:
      - "127.0.0.1:9000:9000"
    restart: unless-stopped

  nginx:
    image: nginx:1.27-alpine
    container_name: job-scraper-nginx
    depends_on:
      - job-scraper
      - monitor
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx/home-local.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/nginx/htpasswd:/etc/nginx/htpasswd:ro
      - ./deploy/nginx/certs:/etc/nginx/certs:ro
    networks:
      - scraper-network
    restart: unless-stopped

  # -------------------------------------------------------------------------
  # Pha 3 placeholders — uncomment once src/api, frontend/, src/ml land.
  # -------------------------------------------------------------------------
  #
  # api:
  #   build:
  #     context: .
  #     dockerfile: src/api/Dockerfile
  #   environment:
  #     DB_HOST: postgres
  #     DB_PASSWORD: ${POSTGRES_PASSWORD}
  #   ports:
  #     - "127.0.0.1:8001:8001"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - scraper-network
  #   restart: unless-stopped
  #
  # frontend:
  #   build:
  #     context: ./frontend
  #   environment:
  #     NEXT_PUBLIC_API_URL: /api
  #   ports:
  #     - "127.0.0.1:3000:3000"
  #   networks:
  #     - scraper-network
  #   restart: unless-stopped
  #
  # ml-inference:
  #   build:
  #     context: .
  #     dockerfile: src/ml/Dockerfile
  #   runtime: nvidia
  #   environment:
  #     NVIDIA_VISIBLE_DEVICES: all
  #     NVIDIA_DRIVER_CAPABILITIES: compute,utility
  #   ports:
  #     - "127.0.0.1:8002:8002"
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   networks:
  #     - scraper-network
  #   restart: unless-stopped
